/*                             -  Exercício Prático-
Leia um vetor de 10 números inteiros dados pelo usuário. Exiba os elementos do
vetor na ordem inversa à leitura: do último ao primeiro número elemento.*/

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        //inicialização do vetor
        int vetor[] = new int[10];
//utilizando  comando "for" paravarredura do vetor que sera digitado pelo usuário    
        for (int i = 0; i < vetor.length; i++) {
            vetor[i] = input.nextInt();
        }
//utilizando comando "for" para organizar e imprimir  dados digitados pelo usuário  na ordem contrária que foi digitado.
        for (int i = vetor.length - 1; i >= 0; i--) {

            System.out.println(vetor[i]);
        }

    }

}
